import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Use default server output to enable SSR, ISR, middleware, and route handlers
  reactStrictMode: true,
};

export default nextConfig;
